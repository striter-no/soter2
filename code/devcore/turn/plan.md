## TURN

Зеркало для пира, сервер не является видимой компонентой сети. Участники сети должны взаимодействовать с сервером TURN, думая, что общаются с другими клиентами. То, что они общаются с TURN им знать не обязательно (и не нужно)

- [x] Сделать принятие соединения `TURN_OPEN_HOLE` (на вход `addr:UID` куда был направлен пакет для соединения).
- [x] Сделать закрытие соединения `TURN_CLOSE_HOLE`
  - [x] На выход TURN сервер отвечает `SYSTEM_MSG` со статусом `OK`/`FAIL`
  - [x] `S2` система смотрит на SYSTEM_MSG и выдает ошибки пользователю
  - [x] TURN сервер слушает OPEN/CLOSE _HOLE сообщения и создает пары `HASH(UID_FROM, UID_TO)` <-> `ADDR:UID`
  
- [ ] Надо решить, как сделать перенаправление трафика для TURN и НЕ-TURN соединений на клиенте:
  - [ ] Сделать фильтр TURN, на `send` командах смотреть TURN'утые UID и перенаправлять их на TURN при необходимости
  - [x] Сделать логи запросов? Как-то обрабатывать SYSTEM_MSG `FAIL/OK` -> каждый реквест хранится до N количества со своим номером 
    - [x] (+ номер реквеста в TURN пакете и + последний отправленный реквест в TURN клиенте)

- [x] Когда S2 (handler) система принимает TURN пакет с `TURN_DATA_MSG` она должна перенаправлять его в listener (для дальнейшей обработки с разделением на watcher и rudp), убирая TURN обертку

- [x] При принятии `TURN_DATA_MSG` на сервере вызывается поиск пары по хешу~~, при ненахождении отправляется `TURN_SYSTEM_MSG(FAIL)`, при успехе ничего?~~
- [x] При принятии не-TURN пакета делается быстрый поиск по `HASH(UID_TO, UID_FROM)` (обратный порядок, т.к. пакет от адресата, а не отправителя). 
  - [x] Если есть пара, то делается TURN пакет с `TURN_DATA_MSG` для ответа

```

// OPEN HOLE
C1 -> turn[ADDR:X] -> TURN ... C2
C1 <- turn[OK] <- PCKT ... C2

// DATA PACKETS
C1 -> turn[pack] -> TURN,pack->UID:ADDR -> pack -> C2
C1 <- turn[pack] <- TURN <- pack <- C2

```
