[project]
name = soter2_gossip
version = 0.0.1

[files]
sources = *.c
output = gossip

[dirs]
includes = include ../base/include ../protocol/include ../lownet/include ../crypto/include
src = src
output = lib

[modules]
base = ../base, abs.conf
proto= ../protocol, abs.conf

[compiler]
build = static
cleanup = false

[flags]
common = -std=c11 -Wall -Wextra -Wpedantic -D_GNU_SOURCE

[modes]
active = debug

[mode.debug]
flags = -g -O0

[mode.release]
flags = -O3
